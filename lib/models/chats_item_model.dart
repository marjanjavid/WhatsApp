class ChatsItemModel {
  String avatar;
  String name;
  String dateTime;
  String lastMessage;
  int chatID;
  ChatsItemModel(
      this.avatar, this.name, this.dateTime, this.lastMessage, this.chatID);
}
